import 'package:flutter/material.dart';

class ChosenPage extends StatelessWidget {
  const ChosenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Chosen page'),);
  }
}